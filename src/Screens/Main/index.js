import React from 'react'
import { View } from 'react-native'
import { TodoList } from '../../Components/TodoList'

export const Main = () => {
    return (
        <View>
            <TodoList/>
        </View>
    );
}